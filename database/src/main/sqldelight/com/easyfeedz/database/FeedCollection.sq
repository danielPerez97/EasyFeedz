CREATE TABLE FeedCollection(
    _id INTEGER NOT NULL,
    feed_id INTEGER,
    name TEXT NOT NULL,
    url TEXT NOT NULL,
    PRIMARY KEY(_id, feed_id),
    FOREIGN KEY (feed_id) REFERENCES Feeds(_id)
);

selectAll:
SELECT * FROM FeedCollection;